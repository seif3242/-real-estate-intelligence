# Used by docker-compose to provision the Postgres container.
# Keep these in sync with backend/.env's DATABASE_URL credentials.
POSTGRES_USER=real_estate_ai
POSTGRES_PASSWORD=change-me
POSTGRES_DB=real_estate_ai
